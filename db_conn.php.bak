<?php

$sname= "localhost";
$unmae= "root";
$password = "";

$db_name = "test_db";

$conn = mysqli_connect($sname, $unmae, $password, $attendance, $db_name);

if (!$conn) {
	echo "Connection failed!";
}